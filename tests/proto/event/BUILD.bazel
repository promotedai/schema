TESTS = glob(["test_*.py"])
DEPS = [
    "//proto/event:event_py_proto",
]

[ py_test(
        name = src[:-3],
        srcs = [src],
        visibility = ["//visibility:public"],
        deps = DEPS,
        legacy_create_init = False,
) for src in TESTS ]
