TESTS = glob(["test_*.py"])
DEPS = [
    "//proto/common:common_py_proto",
    "//tests/proto/util",
]

[ py_test(
        name = src[:-3],
        srcs = [src],
        visibility = ["//visibility:public"],
        deps = DEPS,
) for src in TESTS ]
